#include <time.h>
#include <stdio.h>
/**
 * _abs - C is so cool
 * @a: to give the absolute value
 * Return: The return here is 0
 */
int _abs(int a)
{
	if (a >= 0)
	return (a);
	else
	return (-a);
}
