#include "main.h"
#include<stdio.h>
/**
 * _isupper - C is so cool
 * @a: the number to consider
 * Return: The return here is 0
 */
int _isupper(int a)
{
	if (a >= 65 && a <= 90)
	{
		return (1);
	}
	else
		return (0);
}
