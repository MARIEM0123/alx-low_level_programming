#include "main.h"
#include<stdio.h>
/**
 * print_line - C is so cool
 * @n: the parameter to be considered
 * Return: The return here is 0
 */

void print_line(int n)
{
	int j;

	if (n <= 0)
		_putchar('\n');
	else
	{
	for (j = 0; j < n; j++)
{
	putchar (95);
}
putchar ('\n');
}
}
