#include "main.h"
#include "main.h"
#include<stdio.h>
/**
 * _isdigit - C is so cool
 * @a: the number to consider
 * Return: The return here is 0
 */
int _isdigit(int a)
{
	if (a >= 48 && a <= 57)
		return (1);
	else
		return (0);
}
