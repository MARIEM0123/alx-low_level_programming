#include "main.h"
#include<stdio.h>

/**
 * mul - C is so cool
 * @a: the first number to consider
 * @b: the second number to consider
 * Return: The return here is 0
 */

int mul(int a, int b)
{
	return (a * b);
}
